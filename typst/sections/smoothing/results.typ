#align(center, 
  grid(rows: (45%, 45%),
      gutter: 4%,
      locate(loc => query(<original>, loc).first()),
      [#figure(image("/img/smoothed.png"), caption: [Imagem suavizada final])],
    )
  )
